schema: spec-driven

context: |
  Tech stack: none detected; bootstrap repository
  Architecture: no application source files found
  Testing: no test runner or quality tooling detected
  Style: no project conventions detected yet

strict_tdd: false

testing:
  detected: 2026-06-24
  runner:
    command: ""
    framework: none
  layers:
    unit:
      available: false
      tool: "—"
    integration:
      available: false
      tool: "—"
    e2e:
      available: false
      tool: "—"
  coverage:
    available: false
    command: "—"
  quality_tools:
    linter:
      available: false
      command: "—"
    type_checker:
      available: false
      command: "—"
    formatter:
      available: false
      command: "—"

rules:
  proposal:
    - Include rollback notes for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Document architecture decisions with rationale
  tasks:
    - Group by phase and keep tasks session-sized
  apply:
    - Follow existing code patterns when code exists
    - Prefer strict TDD when a runner is introduced
  verify:
    - Record the exact commands used for validation
  archive:
    - Warn before merging destructive deltas
